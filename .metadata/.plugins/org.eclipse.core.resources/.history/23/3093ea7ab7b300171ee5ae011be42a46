package com.chenming.test;

public class ToUpperCase
{
	public static void main(String[] args)
	{
		String str = "http://localhost/search/search_offices.jsp";
		/*String[] strArray = str.split("/");
		for(int i=0;i<strArray.length;i++)
		{
			System.out.println("第 "+i+" 个: "+strArray[i].toUpperCase());
		}*/
		
		int begin = str.indexOf(":");
		System.out.println(begin);
		int index = str.lastIndexOf(".");
		String newStr = str.substring(7, index).replaceAll("/", "_").toUpperCase();
		System.out.println(newStr);
		
		
		/*String newStr = str.replaceAll("/", "_");
		System.out.println(newStr.toUpperCase());*/
	}
}
